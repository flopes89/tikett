import React from "react";
import { Container, Row, Col } from "reactstrap";

export default () => (
    <Container id="root">
        <Row>
            <Col>
                <h1>OFTS</h1>
            </Col>
        </Row>
    </Container>
);
