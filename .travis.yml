language: node_js
node_js: 12
os:
  - linux
  - windows
  - osx

osx_image: xcode10.2

cache: 
  directories:
    - node_modules
    - $HOME/.cache/electron
    - $HOME/.cache/electron-builder

script:
  - npm install
  - npm run validate-types
  - npm test
  - npm run build
  - npm run dist
